[1mdiff --git a/backend/runtime/llm_service.py b/backend/runtime/llm_service.py[m
[1mindex 62005e0..7f198a4 100644[m
[1m--- a/backend/runtime/llm_service.py[m
[1m+++ b/backend/runtime/llm_service.py[m
[36m@@ -16,8 +16,6 @@[m [mimport logging[m
 [m
 import httpx[m
 [m
[31m-from adapters.gigachat_client import get_gigachat_client[m
[31m-[m
 logger = logging.getLogger("padplus.llm")[m
 [m
 [m
[36m@@ -61,8 +59,8 @@[m [masync def acompletion(response: Any) -> Any:[m
             if asyncio.iscoroutine(data):[m
                 data = await data[m
             return data[m
[31m-    except Exception as e:[m
[31m-        logger.warning(f"{__name__} error: {e}")[m
[32m+[m[32m    except Exception:[m
[32m+[m[32m        pass[m
     return response[m
 [m
 [m
[36m@@ -74,8 +72,8 @@[m [mdef _object_to_dict(obj: Any) -> Dict[str, Any]:[m
     try:[m
         if hasattr(obj, "items") and callable(obj.items):[m
             return dict(obj.items())[m
[31m-    except Exception as e:[m
[31m-        logger.warning(f"{__name__} error: {e}")[m
[32m+[m[32m    except Exception:[m
[32m+[m[32m        pass[m
     result: Dict[str, Any] = {}[m
     for attr in dir(obj):[m
         if attr.startswith("_"):[m
[36m@@ -91,6 +89,10 @@[m [mdef _object_to_dict(obj: Any) -> Dict[str, Any]:[m
 [m
 [m
 OPENROUTER_BASE = "https://openrouter.ai/api/v1"[m
[32m+[m[32m# GigaChat API endpoints[m
[32m+[m[32m# GigaChat v2 использует новый auth URL без порта 9443[m
[32m+[m[32mGIGACHAT_AUTH_URL = os.getenv("GIGACHAT_AUTH_URL", "https://ngw.devices.sberbank.ru:9443/api/v2/oauth")[m
[32m+[m[32mGIGACHAT_API_URL = os.getenv("GIGACHAT_API_URL", "https://gigachat.devices.sberbank.ru/api/v1/chat/completions")[m
 [m
 DEFAULT_MODELS = {[m
     "gigachat": "GigaChat",[m
[36m@@ -109,6 +111,7 @@[m [mDEFAULT_MODELS = {[m
 [m
 # Бесплатные модели OpenRouter[m
 OPENROUTER_FREE_MODELS = [[m
[32m+[m[32m    "meta-llama/llama-3.1-8b-instruct:free",[m
     "microsoft/phi-3-mini-4k-instruct:free",[m
     "google/gemma-2b-it:free",[m
     "mistralai/mistral-7b-instruct:free",[m
[36m@@ -138,12 +141,6 @@[m [mclass LLMService:[m
         stream: bool = False,[m
         **kwargs,[m
     ) -> LLMResponse:[m
[31m-        """[m
[31m-        Отправляет запрос к указанному провайдеру.[m
[31m-        [m
[31m-        ProviderManager отвечает за выбор провайдера и fallback.[m
[31m-        LLMService только маршрутизирует к конкретному HTTP-провайдеру.[m
[31m-        """[m
         key = api_key or self.default_api_key[m
         model_name = model or self.default_model[m
         if not key:[m
[36m@@ -183,36 +180,15 @@[m [mclass LLMService:[m
         if provider and provider != "openrouter":[m
             base_url = OPENROUTER_BASE[m
 [m
[31m-        # OpenRouter не добавляет openrouter/ к имени модели — он принимает[m
[31m-        # либо полный ID (openai/gpt-4o-mini), либо короткое имя (gpt-4o-mini)[m
[31m-        # "auto" → используем модель по умолчанию[m
[31m-        if model in ("auto", f"{provider}/auto"):[m
[31m-            model = None[m
[31m-[m
[31m-        if provider == "openrouter":[m
[31m-            if model and "/" in model:[m
[31m-                if model.startswith("openrouter/"):[m
[31m-                    model = model[len("openrouter/"):][m
[31m-                full_model = model[m
[31m-            elif model:[m
[31m-                full_model = model[m
[31m-            elif DEFAULT_MODELS.get("openrouter"):[m
[31m-                full_model = DEFAULT_MODELS["openrouter"][m
[31m-            else:[m
[31m-                full_model = "gpt-4o-mini"[m
[32m+[m[32m        if model and "/" not in model and provider:[m
[32m+[m[32m            full_model = f"{provider}/{model}"[m
         else:[m
[31m-            if model and "/" not in model and provider:[m
[31m-                full_model = f"{provider}/{model}"[m
[31m-            elif not model:[m
[31m-                model_for_provider = DEFAULT_MODELS.get(provider) if provider else None[m
[31m-                full_model = model_for_provider or DEFAULT_MODELS.get("openrouter", "gpt-4o-mini")[m
[31m-            else:[m
[31m-                full_model = model or DEFAULT_MODELS.get("openrouter", "gpt-4o-mini")[m
[32m+[m[32m            full_model = model or (DEFAULT_MODELS.get(provider) if provider else DEFAULT_MODELS.get("openrouter", "gpt-4o-mini"))[m
 [m
[31m-        if full_model:[m
[31m-            if ":free" not in full_model and os.getenv("USE_FREE_MODELS", "false").lower() == "true":[m
[31m-                full_model = OPENROUTER_FREE_MODELS[0][m
[31m-                logger.info(f"Using free OpenRouter model: {full_model}")[m
[32m+[m[32m        # Если модель не содержит ":free", но пользователь хочет бесплатные модели[m
[32m+[m[32m        if ":free" not in full_model and os.getenv("USE_FREE_MODELS", "false").lower() == "true":[m
[32m+[m[32m            full_model = OPENROUTER_FREE_MODELS[0][m
[32m+[m[32m            logger.info(f"🆓 Using free OpenRouter model: {full_model}")[m
 [m
         messages = [][m
         if system_prompt:[m
[36m@@ -243,12 +219,11 @@[m [mclass LLMService:[m
 [m
         status_code = getattr(response, "status_code", None)[m
         if isinstance(status_code, int) and status_code != 200:[m
[31m-            try:[m
[31m-                raw = str(getattr(response, "content", b""), "utf-8", errors="replace")[:500][m
[31m-            except Exception:[m
[31m-                raw = ""[m
[31m-            error_text = raw.encode("ascii", errors="replace").decode("ascii")[m
[31m-            logger.error(f"HTTP error {status_code}: {error_text}")[m
[32m+[m[32m            # Ошибки OpenRouter могут содержать NBSP (\xa0) и др. не-ascii символы.[m
[32m+[m[32m            # Никаких принудительных ascii-перекодировок: оставляем UTF-8 preview.[m
[32m+[m[32m            raw_text = getattr(response, "text", "") or ""[m
[32m+[m[32m            error_text = (raw_text[:500]).encode("utf-8", errors="replace").decode("utf-8", errors="replace")[m
[32m+[m[32m            logger.error(f"❌ HTTP error {status_code}: {error_text}")[m
             raise ValueError(f"Ошибка API: {status_code} - {error_text}")[m
 [m
         data = await acompletion(response)[m
[36m@@ -282,27 +257,225 @@[m [mclass LLMService:[m
         temperature: float = 0.7,[m
         max_tokens: Optional[int] = None,[m
     ) -> LLMResponse:[m
[32m+[m[32m        """[m
[32m+[m[32m        Отправляет запрос к GigaChat API.[m
[32m+[m
[32m+[m[32m        Flow:[m
[32m+[m[32m        1. Определить модель[m
[32m+[m[32m        2. Получить access_token через Basic Auth[m
[32m+[m[32m        3. Отправить chat completion запрос[m
[32m+[m
[32m+[m[32m        Args:[m
[32m+[m[32m            prompt: Текст запроса[m
[32m+[m[32m            system_prompt: Системный промпт[m
[32m+[m[32m            api_key: Ключ API в формате "client_id:secret" или готовый base64[m
[32m+[m[32m            model: Название модели[m
[32m+[m[32m            temperature: Температура генерации[m
[32m+[m[32m            max_tokens: Максимальное количество токенов[m
[32m+[m
[32m+[m[32m        Returns:[m
[32m+[m[32m            LLMResponse с ответом[m
[32m+[m[32m        """[m
[32m+[m[32m        import base64[m
[32m+[m[32m        import uuid[m
[32m+[m
         model_name = self._resolve_gigachat_model(model or "GigaChat")[m
[31m-        logger.info(f"GigaChat call: model={model_name}, prompt_len={len(prompt)}")[m
[32m+[m[32m        logger.info(f"🔌 GigaChat call: model={model_name}, prompt_len={len(prompt)}")[m
[32m+[m
[32m+[m[32m        # === ШАГ 1: Подготовка HTTP-клиента для GigaChat ===[m
[32m+[m[32m        # GigaChat использует самоподписанные сертификаты, SSL verify по умолчанию отключён[m
[32m+[m[32m        gigachat_verify_tls = os.getenv("GIGACHAT_VERIFY_TLS", "false").strip().lower() not in ("0", "false", "no")[m
[32m+[m[32m        logger.info(f"🔒 GigaChat SSL verification: {gigachat_verify_tls}")[m
[32m+[m
[32m+[m[32m        # Создаём отдельную сессию для GigaChat (с verify=false)[m
[32m+[m[32m        # Используем свой клиент, т.к. self._session может иметь verify=true[m
[32m+[m[32m        async with httpx.AsyncClient([m
[32m+[m[32m            timeout=httpx.Timeout(30.0, connect=15.0),[m
[32m+[m[32m            verify=gigachat_verify_tls,[m
[32m+[m[32m        ) as gigachat_session:[m
[32m+[m
[32m+[m[32m            # === ШАГ 2: Подготовка Basic Auth заголовка ===[m
[32m+[m[32m            # Ключ GigaChat приходит из БД в формате "client_id:secret"[m
[32m+[m[32m            # Для Basic Auth нужно base64(<client_id>:<secret>)[m
[32m+[m[32m            if api_key and ":" in api_key:[m
[32m+[m[32m                # Ключ формата "client_id:secret" — кодируем в base64[m
[32m+[m[32m                raw_key = api_key.strip()[m
[32m+[m[32m                logger.info(f"🔑 GigaChat key format: 'client_id:secret', encoding to base64 (len={len(raw_key)})")[m
[32m+[m[32m                encoded_key = base64.b64encode(raw_key.encode()).decode()[m
[32m+[m[32m            elif api_key and len(api_key) > 80:[m
[32m+[m[32m                # Если ключ уже выглядит как base64 (длинная строка)[m
[32m+[m[32m                encoded_key = api_key.strip()[m
[32m+[m[32m                logger.info(f"🔑 GigaChat key format: pre-encoded (len={len(api_key)})")[m
[32m+[m[32m            else:[m
[32m+[m[32m                # Пробуем как есть[m
[32m+[m[32m                encoded_key = base64.b64encode(api_key.encode()).decode() if api_key else ""[m
[32m+[m[32m                logger.info(f"🔑 GigaChat key format: raw, encoding to base64 (len={len(api_key or '')})")[m
[32m+[m
[32m+[m[32m            if not encoded_key:[m
[32m+[m[32m                raise ValueError("GigaChat: API ключ пуст")[m
[32m+[m
[32m+[m[32m            rquid = str(uuid.uuid4()).replace("-", "")[m
[32m+[m[32m            auth_headers = {[m
[32m+[m[32m                "Content-Type": "application/x-www-form-urlencoded",[m
[32m+[m[32m                "Accept": "application/json",[m
[32m+[m[32m                "RqUID": rquid,[m
[32m+[m[32m                "Authorization": f"Basic {encoded_key}",[m
[32m+[m[32m            }[m
[32m+[m[32m            logger.info(f"🔑 GigaChat auth headers: RqUID={rquid[:8]}..., Authorization=Basic ...{encoded_key[-8:]}")[m
[32m+[m
[32m+[m[32m            # === ШАГ 3: Получение access_token ===[m
[32m+[m[32m            logger.info(f"📡 GigaChat: requesting access_token from {GIGACHAT_AUTH_URL}")[m
[32m+[m
[32m+[m[32m            max_retries = 2[m
[32m+[m[32m            last_error = None[m
[32m+[m[32m            token_resp = None[m
[32m+[m
[32m+[m[32m            for attempt in range(max_retries):[m
[32m+[m[32m                try:[m
[32m+[m[32m                    token_resp = await gigachat_session.post([m
[32m+[m[32m                        GIGACHAT_AUTH_URL,[m
[32m+[m[32m                        headers=auth_headers,[m
[32m+[m[32m                        data={"scope": "GIGACHAT_API_PERS"},[m
[32m+[m[32m                    )[m
[32m+[m[32m                    status = token_resp.status_code[m
[32m+[m[32m                    logger.info(f"📡 GigaChat auth response: status={status}, attempt={attempt+1}/{max_retries}")[m
[32m+[m
[32m+[m[32m                    if status == 200:[m
[32m+[m[32m                        break  # Успешно[m
[32m+[m[32m                    elif status == 401:[m
[32m+[m[32m                        raise ValueError(f"GigaChat auth failed: 401 Unauthorized — проверьте правильность ключа")[m
[32m+[m[32m                    elif status == 400:[m
[32m+[m[32m                        # 400 Bad Request — проблема с форматом запроса[m
[32m+[m[32m                        error_body = token_resp.text[:500] if hasattr(token_resp, 'text') else "no body"[m
[32m+[m[32m                        # Пробуем прочитать json[m
[32m+[m[32m                        try:[m
[32m+[m[32m                            error_json = token_resp.json()[m
[32m+[m[32m                            error_body = str(error_json)[:500][m
[32m+[m[32m                        except Exception:[m
[32m+[m[32m                            pass[m
[32m+[m[32m                        logger.error(f"❌ GigaChat auth 400 Bad Request. Body: {error_body}")[m
[32m+[m[32m                        raise ValueError([m
[32m+[m[32m                            f"GigaChat auth failed: 400 Bad Request. "[m
[32m+[m[32m                            f"Проверьте правильность формата ключа (должен быть 'client_id:secret'). "[m
[32m+[m[32m                            f"Ответ сервера: {error_body}"[m
[32m+[m[32m                        )[m
[32m+[m[32m                    elif status >= 500:[m
[32m+[m[32m                        last_error = f"GigaChat auth failed: {status} (server error, attempt {attempt+1})"[m
[32m+[m[32m                        logger.warning(f"⚠️ {last_error}")[m
[32m+[m[32m                        if attempt + 1 < max_retries:[m
[32m+[m[32m                            await asyncio.sleep(1 * (attempt + 1))[m
[32m+[m[32m                            continue[m
[32m+[m[32m                        raise ValueError(last_error)[m
[32m+[m[32m                    else:[m
[32m+[m[32m                        error_body = token_resp.text[:500] if hasattr(token_resp, 'text') else "no body"[m
[32m+[m[32m                        raise ValueError(f"GigaChat auth failed: {status} {error_body}")[m
[32m+[m[32m                except httpx.ConnectError as e:[m
[32m+[m[32m                    last_error = f"GigaChat connection error (attempt {attempt+1}): {e}"[m
[32m+[m[32m                    logger.warning(f"⚠️ {last_error}")[m
[32m+[m[32m                    if attempt + 1 < max_retries:[m
[32m+[m[32m                        await asyncio.sleep(2 * (attempt + 1))[m
[32m+[m[32m                        continue[m
[32m+[m[32m                    raise ValueError(f"GigaChat недоступен: не удалось подключиться к {GIGACHAT_AUTH_URL}. "[m
[32m+[m[32m                                     f"Проверьте подключение к интернету и доступность сервера.")[m
[32m+[m[32m                except httpx.TimeoutException as e:[m
[32m+[m[32m                    last_error = f"GigaChat auth timeout (attempt {attempt+1}): {e}"[m
[32m+[m[32m                    logger.warning(f"⚠️ {last_error}")[m
[32m+[m[32m                    if attempt + 1 < max_retries:[m
[32m+[m[32m                        await asyncio.sleep(2 * (attempt + 1))[m
[32m+[m[32m                        continue[m
[32m+[m[32m                    raise ValueError(f"GigaChat auth timeout: сервер не отвечает. Попробуйте позже.")[m
 [m
[31m-        client = get_gigachat_client()[m
[31m-        result = await client.complete([m
[31m-            prompt=prompt,[m
[31m-            system_prompt=system_prompt,[m
[31m-            api_key=api_key,[m
[31m-            model=model_name,[m
[31m-            temperature=temperature,[m
[31m-            max_tokens=max_tokens,[m
[31m-        )[m
[32m+[m[32m            if token_resp is None:[m
[32m+[m[32m                raise ValueError(f"GigaChat auth failed: не удалось получить ответ от сервера после {max_retries} попыток")[m
 [m
[31m-        return LLMResponse([m
[31m-            text=result["text"],[m
[31m-            model=result["model"],[m
[31m-            provider=result["provider"],[m
[31m-            usage=result["usage"],[m
[31m-            finish_reason=result.get("finish_reason"),[m
[31m-            metadata={"raw_response": result.get("raw_response", {})},[m
[31m-        )[m
[32m+[m[32m            token_data = token_resp.json()[m
[32m+[m[32m            access_token = token_data.get("access_token")[m
[32m+[m[32m            if not access_token:[m
[32m+[m[32m                raise ValueError(f"GigaChat auth did not return access_token. Response: {token_data}")[m
[32m+[m
[32m+[m[32m            token_expires_at = token_data.get("expires_at", 0)[m
[32m+[m[32m            logger.info(f"✅ GigaChat access_token получен (expires_at={token_expires_at})")[m
[32m+[m
[32m+[m[32m            # === ШАГ 4: Отправка chat completion ===[m
[32m+[m[32m            messages = [][m
[32m+[m[32m            if system_prompt:[m
[32m+[m[32m                messages.append({"role": "system", "content": system_prompt})[m
[32m+[m[32m            messages.append({"role": "user", "content": prompt})[m
[32m+[m
[32m+[m[32m            chat_headers = {[m
[32m+[m[32m                "Authorization": f"Bearer {access_token}",[m
[32m+[m[32m                "Content-Type": "application/json",[m
[32m+[m[32m            }[m
[32m+[m[32m            chat_body = {[m
[32m+[m[32m                "model": model_name,[m
[32m+[m[32m                "messages": messages,[m
[32m+[m[32m                "temperature": temperature,[m
[32m+[m[32m            }[m
[32m+[m[32m            if max_tokens:[m
[32m+[m[32m                chat_body["max_tokens"] = max_tokens[m
[32m+[m[32m            if os.getenv("GIGACHAT_PROFANITY_CHECK", "false").lower() == "true":[m
[32m+[m[32m                chat_body["profanity_check"] = True[m
[32m+[m
[32m+[m[32m            logger.info(f"📡 GigaChat: sending chat completion (model={model_name}, messages={len(messages)})")[m
[32m+[m
[32m+[m[32m            chat_resp = None[m
[32m+[m
[32m+[m[32m            for attempt in range(max_retries):[m
[32m+[m[32m                try:[m
[32m+[m[32m                    chat_resp = await gigachat_session.post([m
[32m+[m[32m                        GIGACHAT_API_URL,[m
[32m+[m[32m                        json=chat_body,[m
[32m+[m[32m                        headers=chat_headers,[m
[32m+[m[32m                    )[m
[32m+[m[32m                    logger.info(f"📡 GigaChat chat response: status={chat_resp.status_code}, attempt={attempt+1}/{max_retries}")[m
[32m+[m
[32m+[m[32m                    if chat_resp.status_code == 200:[m
[32m+[m[32m                        break[m
[32m+[m[32m                    elif chat_resp.status_code == 401:[m
[32m+[m[32m                        raise ValueError(f"GigaChat API error: 401 Unauthorized — access_token истёк или недействителен")[m
[32m+[m[32m                    elif chat_resp.status_code >= 500:[m
[32m+[m[32m                        last_error = f"GigaChat API error: {chat_resp.status_code} (server error, attempt {attempt+1})"[m
[32m+[m[32m                        logger.warning(f"⚠️ {last_error}")[m
[32m+[m[32m                        if attempt + 1 < max_retries:[m
[32m+[m[32m                            await asyncio.sleep(1 * (attempt + 1))[m
[32m+[m[32m                           